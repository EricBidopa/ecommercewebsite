import React from "react";
import '../styles.css'


export default function Navbar(){
    return(
        <section>
            <nav>
                <h1 className="navfirsttext">TThis is Just the beginning</h1>
                <h2>This the the start of something big RENDAR</h2>
            </nav>
        </section>

    );
}